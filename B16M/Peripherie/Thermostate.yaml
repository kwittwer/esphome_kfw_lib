# ============================================
# Thermostate Basis
# Enthält: externe Component, globale Schalter und Master-Sensorik
# 
# Die eigentlichen Thermostat-Instanzen werden direkt im jeweiligen
# Setup unter packages: als !include thermostat_template.yaml eingebunden.
# ============================================

external_components:
  - source:
      type: local
      path: B16M_components/components
    components: [dummy_thermostat]

# ============================================
# Global Switches (für alle Thermostaten)
# ============================================

switch:
  - platform: template
    name: "Lokale Heizregelung"
    id: local_control_switch
    entity_category: config
    optimistic: true
    restore_mode: RESTORE_DEFAULT_OFF
    
  - platform: template
    name: "Heizung Hauptschalter"
    id: heating_master_switch
    restore_mode: RESTORE_DEFAULT_OFF
    entity_category: config
    optimistic: true

# ============================================
# Global Binary Sensor (für Heizung Master-Switch)
# ============================================

binary_sensor:
  - platform: template
    name: "Heizung Hauptschalter"
    id: hw_master_sw
    entity_category: diagnostic
    lambda: |-
      if (id(DI1).state == true) {
        return true;
      } else {
        return false;
      }
